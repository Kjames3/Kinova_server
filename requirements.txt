fastapi>=0.110.0
uvicorn[standard]>=0.29.0
numpy>=1.24.0
psutil>=5.9.0
opencv-python>=4.9.0
pyrealsense2>=2.54.0
depthai>=2.24.0
# kortex_api is installed separately — see install.sh
#
# ROS2 (Part 3) is NOT pip-installed: rclpy / cv_bridge / rosidl_runtime_py come
# from the system ROS2 Humble install. Source /opt/ros/humble/setup.bash (and the
# kortex workspace overlay) before launching, and create the venv with
# --system-site-packages so those packages remain importable. The server runs
# fine without ROS2 — the ROS2 dashboard panel just reports BRIDGE OFFLINE.
